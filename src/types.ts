export type Topic = {
  title: string;
  path: string;
};

export type News = {
  title: string;
  category: string;
  date: string;
  path: string;
};

export type Procurement = {
  title: string;
  category: string;
  date: string;
  path: string;
};
